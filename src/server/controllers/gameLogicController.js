const db = require('../db/database');

// A implementar logica de gerenciamento da partida de jogo (verificar acertos e erros, atualizar pontuação, solicitar geração de grafos ,solicitar finalização partida, etc.)
